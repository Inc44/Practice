# a
The parent will finish before the child process. The terminal will return input before the child process ends, which may garble the output.
# b
Use `wait(NULL)` or `waitpid()` in the parent to wait for the child process to finish.